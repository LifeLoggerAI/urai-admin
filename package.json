{
  "name": "urai-admin-root",
  "private": true,
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "deploy": "firebase deploy --only hosting,functions,firestore,storage -P urai-4dc1d",
    "smoke-test": "bash scripts/smoke-test.sh"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "firebase-tools": "^13.13.2"
  }
}
