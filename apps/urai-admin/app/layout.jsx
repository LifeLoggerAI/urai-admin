export var metadata = {
    title: "URAI Admin",
    description: "URAI Admin Console",
};
export default function RootLayout(_a) {
    var children = _a.children;
    return (<html lang="en">
      <body>{children}</body>
    </html>);
}
