import { PageFrame } from '@/components/marketing';

export default function PrivacyPage() {
  return (
    <PageFrame>
      <main className="page section">
        <p className="eyebrow">Privacy</p><h1>Privacy policy placeholder for V1 launch review.</h1>
        <div className="card"><p>URAI Analytics minimizes analytics collection, requires consent snapshots on event ingestion, applies sensitive-key redaction, scopes data by organization and workspace, and supports export/delete workflows in the V1 data model. Replace this placeholder with counsel-reviewed privacy policy text before public launch.</p></div>
      </main>
    </PageFrame>
  );
}
